# 定时录音功能方案

> 编制时间：2026-08-26
> 项目：RadioDroid v1.06
> 状态：方案设计（未动代码）
> 立场：不完全推翻用户原方案，但有几处关键修正 + 大量边界情况补全

---

## 一、对用户原方案的评估与关键修正

用户原方案：点录音键 → 2 选 1（直接录 / 选开始结束时间）→ 确定后"闪过提示保证存储空间" → 到点开始 → 到点结束。

### 修正 1（核心）：定时录音必须绑定一个电台

原方案只说"开始结束时间"，没说**录哪个台**。定时录音的本质是"无人值守录某台某时段"，到点时"当前台"是什么不可控（用户可能已切台）。因此定时录音任务必须像闹钟一样**绑定一个指定电台**，到点自动切到该台播放并开录。

**入口设计**（对原"录音键 2 选 1"的增强）：
- **快捷入口**（保留用户原意）：录音键 → 2 选 1
  - 选项 A「立即录音」：原行为，录当前正在播放的台
  - 选项 B「定时录音」：弹出设置对话框，**默认绑定当前台**（可在对话框内改选其他台）+ 开始/结束时间
- **完整入口**（新增）：电台详情页 →「添加定时录音」（录任意台）；录音管理页 →「定时录音」标签（管理所有定时任务，类似闹钟列表）

> 理由：录音键的快捷场景是"我正在听 A 台，想今晚录它 1 小时"，绑定当前台合理；但要录一个现在没在听的台，必须能选台。两个入口覆盖两种场景。

### 修正 2：存储空间"闪过提示"改为"自动估算 + 强制校验"

原方案"闪过提示保证存储空间"——闪过用户来不及看，且让用户自己保证不现实。改为：
- 设置任务时**自动估算**所需空间 = `电台码率(kbps) × 1000 / 8 × 时长秒数 × 1.1 余量`
  - 码率取本地库 `RadioStation.Bitrate` 字段；缺失时默认 128 kbps
- 与 `StatFs` 读取的录音目录可用空间对比：
  - **不足**：阻止保存，明确告知"预计占用 XX MB，可用 YY MB，请清理或缩短时长"
  - **临界（可用 < 估算 × 1.5）**：警告但允许保存
  - **充足**：在确认对话框显示"预计占用 XX MB"
- 这同时填上了 `RecordingsManager` 里已有的 `//TODO: check available disk space here`

### 修正 3：录音期间必须抑制自动暂停逻辑

本项目有"扬声器零音量暂停""有线耳机断开暂停""蓝牙断开暂停""睡眠定时器停止"。定时录音在后台无人值守时，用户拔耳机/调零音量/睡眠定时器到点 → 触发暂停 → **录音断流**。必须引入"录音会话标记"，录音期间抑制上述自动暂停（仅响应用户显式停止）。

### 修正 4：结束时不只停录音，还要处理播放

定时录音到结束时间：先 `stopRecording()`，再判断播放来源——若该播放是定时录音自动启动的，则停止播放；若用户在录音期间手动切了别的台在听，则保留用户当前播放（只停录音）。需用"播放来源标记"区分。

---

## 二、核心数据结构（复用闹钟模式）

新增 `DataRecordingSchedule`，结构对齐 `DataRadioStationAlarm` 以便复用持久化与重注册逻辑：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 唯一 ID（复用 getFreeId 思路） |
| station | DataRadioStation | 录音目标电台 |
| startHour / startMinute | int | 开始时间 |
| endHour / endMinute | int | 结束时间（闹钟没有，新增） |
| weekDays | List<Integer> | 重复日（空=单次） |
| enabled | boolean | 启用状态 |
| repeating | boolean | 是否重复 |
| startTriggerTime | long | 开始闹钟实际注册时间（错过判断） |
| endTriggerTime | long | 结束闹钟实际注册时间 |
| estimatedBytes | long | 估算占用（设置时算好存住，供执行前再校验） |
| lastResult | String | 上次执行结果（成功/失败原因），用于列表展示 |

持久化走 SharedPreferences + Gson（与闹钟一致），key 前缀 `recsch.`，新增 `RecordingScheduleManager`（仿 `RadioAlarmManager`）。

---

## 三、执行状态机

定时录音任务的生命周期：

```
[已设定 ENABLED]
   │ 到 startTriggerTime（AlarmReceiver 唤醒）
   ▼
[STARTING] ── 获取 wakelock/wifilock（复用 AlarmReceiver 模式）
   │ bindService 拉起 PlayerService
   │ SetStation(目标台) + 标记 playbackSource=RECORDING_SCHEDULE
   │ Play(true) → 等流就绪（Playing 状态）
   │ 抑制自动暂停（recordingSessionActive=true）
   │ startRecording()
   ▼
[RECORDING] ── 前台通知"正在录音：XX 台，将于 HH:mm 结束"
   │ 到 endTriggerTime（第二个闹钟）
   │   或 用户手动停止录音
   │   或 录音异常（流断/写失败）
   ▼
[STOPPING] ── stopRecording() → flush+close 文件
   │ 若 playbackSource==RECORDING_SCHEDULE → Stop()
   │ recordingSessionActive=false（恢复自动暂停）
   │ 更新 lastResult
   │ 单次任务：enabled=false（仿闹钟）；重复任务：重注册下一次
   ▼
[DONE] ── 完成通知（文件路径 + 大小）
```

**关键**：开始与结束是**两个独立的 AlarmManager 任务**，都走 `setAlarmClock`（不受 Doze 限制），都在 `BootReceiver`/`resetAllAlarms` 中重注册。

---

## 四、用户没想到的边界情况（重点）

### A. 权限与前台服务

| 项 | 结论 | 依据 |
|----|------|------|
| RECORD_AUDIO | **不需要** | 录音录的是网络流字节（`onBytesAvailable` 写文件），非麦克风 |
| FOREGROUND_SERVICE_MICROPHONE | **不需要** | 同上，依附 mediaPlayback 前台服务即可 |
| 存储权限 | 需预检 | 现有写公共 `Music/Recordings` 目录，Android 10+ 分区存储下 `WRITE_EXTERNAL_STORAGE` maxSdk=32，Android 11+ 需走 MediaStore（**这是现有隐患，定时录音在后台写会放大，建议同步修**） |
| POST_NOTIFICATIONS | 已声明 | 录音通知需要，Android 13+ 运行时请求 |
| SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM | 已声明 | setAlarmClock 需要 |
| 后台启动前台服务 | 有豁免 | setAlarmClock 触发的广播有后台启动前台服务豁免，现有闹钟已验证可行；录音复用同路径，但 **Android 14+ 需实测** |

**权限预检时机**：设置定时录音任务时就预检存储/通知权限，缺失则当场引导授权——**不能等到后台触发时再弹**（后台无法弹窗）。

### B. 设备重启

- `BootReceiver` 已调 `resetAllAlarms()`。需扩展为同时 `resetAllRecordingSchedules()`，重注册所有 enabled 任务的开始/结束闹钟。
- **重启发生在录音进行中**：流会断（PlayerService 进程没了），录音文件可能不完整。开机后检测到"上次录音未正常结束"（`recordingSessionActive` 持久化标志 + 文件无 EOF 标记）→ 清理半成品文件 + 发通知告知用户"因重启录音中断"。

### C. 自动暂停逻辑冲突（详见修正 3）

录音期间抑制：扬声器零音量暂停、有线耳机断开暂停、蓝牙断开暂停、睡眠定时器停止。用 `recordingSessionActive` 标记门控这些逻辑的入口。

### D. 用户在录音期间手动操作

| 用户操作 | 行为 |
|---------|------|
| 手动点录音停止 | 正常停止，发完成通知（视为用户主动结束） |
| 手动切台 | 录音继续但录的是新台 → **建议：切台即停止录音并通知"因切台录音中止"**，或锁定切换（更安全）。推荐前者 |
| 手动停止播放 | 停止录音（流断了），通知"因播放停止录音中止" |
| 再点立即录音 | 拒绝，提示"已有定时录音进行中" |

### E. 多个定时录音重叠

- 同一时刻只能一个录音在跑。设置新任务时检测与现有 enabled 任务的时间区间是否重叠，重叠则阻止并提示冲突任务。
- 重复任务跨日重叠同理检测。

### F. 时长上限

防止用户设 22:00–次日 22:00（24 小时）录爆磁盘：
- **软上限 8 小时**：超过警告但允许
- **硬上限**：`min(估算空间占用 ≥ 可用空间 90%, 24 小时)` → 阻止
- 结束时间 ≤ 开始时间 → 视为跨日（+24h），需明确提示用户

### G. 时间校验

| 情况 | 处理 |
|------|------|
| 开始时间已过（设定时） | 仿闹钟：顺延到次日（单次）或下一匹配日（重复） |
| 结束时间 ≤ 开始时间 | 视为跨日，结束闹钟时间 = 结束时间 + 24h，UI 明确提示 |
| 开始与结束相同 | 阻止，提示时长为 0 |

### H. 时区 / 改系统时间 / 夏令时

- 复用闹钟的 `Calendar` 本地时间机制，重复任务跨夏令时自动处理。
- 用户手动改系统时间：`setAlarmClock` 基于 RTC 会随之调整，一般无需特殊处理。但若用户把时间往前调导致开始闹钟"已过"→ `load()` 的错过作废机制生效（单次任务作废，重复任务顺延）。

### I. 录音失败处理矩阵

| 失败场景 | 检测 | 处理 |
|---------|------|------|
| 到点电台连不上 | `Play()` 返回 null（仿闹钟的 20 次重试） | 通知"录音失败：无法连接 XX 台"；单次任务作废，重复任务保留等下次 |
| 录音中网络断开 | 流断 → `onRecordingEnded` 提前触发 | 保留已录部分，通知"录音因网络中断提前结束（已录 XX 分）" |
| 存储空间写满 | `onBytesAvailable` 写抛 IOException | `RecordingsManager` 已有 stopRecording 逻辑，补"空间不足"通知 |
| 文件创建失败 | `FileNotFoundException` | 通知"录音失败：无法创建文件"，检查存储权限/路径 |
| 权限被收回（用户在系统设置撤销） | 执行前预检 | 通知"录音失败：缺少存储/通知权限" |

### J. 半成品文件清理

录音异常中断会产生 0 字节或小文件。录音开始时记录文件路径，结束/中断时检查：
- 0 字节 → 删除
- < 10 秒内容 → 标记或询问用户是否删除
- ≥ 10 秒 → 保留并在列表标注"（中断）"

### K. "闪过提示"改为持久确认

设置任务确定后，不只闪 Toast，而是：
- Snackbar/对话框持久显示："已设定定时录音：[电台名] [开始]-[结束]，预计占用 XX MB，将于 [日期 时间] 开始"
- 录音管理列表实时可见该任务及倒计时

### L. 录音完成通知

录音正常结束 → 通知"录音完成：[电台名] [时长] [文件名]，点击打开"。点击跳转录音列表。

### M. 与闹钟的交互

定时录音自动启动播放时，不应触发闹钟的音量渐增（`SetAlarmFade`）。需区分 `playbackSource`：闹钟来源才走渐增，录音来源用默认音量（或用户设置的录音音量）。

### N. 码率缺失的估算

本地库 `RadioStation.Bitrate` 可能为 0/null。估算时：
- 有码率 → 用实际值
- 无码率 → 默认 128 kbps，并在确认提示标注"（按 128kbps 估算，实际以电台为准）"

---

## 五、技术实现概要（不写代码，仅落地路径）

### 复用现有
- `AlarmManager.setAlarmClock()` + `AlarmReceiver` 模式 → 新增 `RecordingScheduleReceiver`（或复用 AlarmReceiver 加 action 区分）
- `RadioAlarmManager` 的持久化/重注册/错过作废逻辑 → 抽公共基类或复制模式到 `RecordingScheduleManager`
- `BootReceiver.resetAllAlarms()` → 扩展调用 `resetAllRecordingSchedules()`
- `PlayerService` 的 `startRecording/stopRecording` AIDL → 直接调用
- `RecordingsManager` 的文件写入/命名 → 直接复用，补空间校验

### 新增
- `DataRecordingSchedule` 数据类 + `RecordingScheduleManager`
- `RecordingScheduleReceiver`（开始/结束两个 action）
- 定时录音设置对话框（电台选择 + 双时间选择器，复用 v1.04 的 `CompactNumberPicker`）
- 录音管理页「定时录音」标签 + 列表适配器（仿 `ItemAdapterRadioAlarm`）
- `PlayerService` 增加 `playbackSource` 标记 + `recordingSessionActive` 标记
- 自动暂停逻辑各入口加 `recordingSessionActive` 门控
- 存储空间估算工具方法（StatFs + 码率）

### AIDL 扩展（IPlayerService）
- `void startScheduledRecording(DataRadioStation station, long endMs)` —— 定时录音专用入口，内部完成"切台→等就绪→开录→注册结束闹钟"
- `void setPlaybackSource(int source)` —— 区分 MANUAL / ALARM / RECORDING_SCHEDULE
- `boolean isRecordingSessionActive()`

---

## 六、与现有功能的交互清单

| 现有功能 | 录音期间行为 |
|---------|-------------|
| 扬声器零音量暂停 | **抑制** |
| 有线耳机断开暂停 | **抑制** |
| 蓝牙断开暂停 | **抑制** |
| 睡眠定时器 | **抑制**（或互斥：设睡眠定时器时若有录音任务冲突则提示） |
| 闹钟 | 录音期间闹钟触发 → 闹钟优先，录音中止并通知 |
| 手动切台 | 录音中止并通知 |
| 手动停止播放 | 录音中止并通知 |
| 均衡器 | 正常工作（不影响录音，录音录的是原始流） |

---

## 七、风险与待确认项

| 项 | 风险 | 建议 |
|----|------|------|
| Android 14+ 后台启动前台服务录音 | 理论有 setAlarmClock 豁免，但录音是"播放+写文件"组合，未实测 | 真机 Android 14+ 实测定时录音完整链路 |
| 公共目录写入（Android 11+） | 现有 `Environment.getExternalStoragePublicDirectory` 在 targetSdk 33 上可能受限 | 建议同步迁移到 MediaStore 写入，定时录音与即时录音共用 |
| setAlarmClock 数量限制 | Android 12+ 每应用 setAlarmClock 有上限（约几百），开始+结束两个闹钟/任务，任务多时可能触限 | 单用户任务量小，风险低；监控 |
| 录音期间系统省电杀进程 | 前台服务保护，但激进省电（华为/小米）可能仍杀 | 录音通知常驻 + 引导用户加白名单 |
| 流码率估算偏差 | 实际码率可能远高于 128kbps（如 320kbps），估算偏低导致中途写满 | 执行前再校验一次可用空间；写满时优雅停止保留已录部分 |

---

## 八、实施建议（分阶段，不含代码）

| 阶段 | 内容 | 依赖 |
|------|------|------|
| P0 | 数据结构 + Manager + 持久化 + 重注册 + BootReceiver 接入 | 仿闹钟，低风险 |
| P1 | 开始/结束 Receiver + PlayerService 切台→等就绪→开录→停录链路 | 核心链路 |
| P2 | 录音期间自动暂停抑制 + playbackSource 标记 | 修正 3/4 |
| P3 | 设置对话框（选台+双时间）+ 录音键 2 选 1 + 录音管理列表 | UI |
| P4 | 存储估算 + 失败处理矩阵 + 半成品清理 + 完成通知 | 健壮性 |
| P5 | 重复任务 + 冲突检测 + 时长上限 | 完善 |
| P6 | 真机验证（Android 14+ 后台触发、重启、断网、拔耳机、写满） | 验收 |

---

## 九、一句话总结

定时录音 = **两个闹钟（开始/结束）+ 录音动作 + 录音会话标记抑制自动暂停**，可大量复用现有闹钟基建；用户原方案的三个主要修正点是：**必须绑定电台**、**存储改为自动估算强制校验**、**录音期间抑制自动暂停**；其余边界情况集中在设备重启、权限预检、失败处理、用户手动操作冲突、时长上限这几类。
